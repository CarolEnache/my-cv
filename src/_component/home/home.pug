.home
  .home-wrapper
    h1.home-name #{curriculum.author.name}
    h2.home-title #{curriculum.author.title}
    ul.home-social
      each social, key in curriculum.author.social
        li.home-item
          label.home-link(for='message-box-id-'+key)
            span(class='resume-icon-' + social.icon)
    include home-message-box
